import { makeStyles } from '@material-ui/core';

const styles = makeStyles({
  'fullscreen-control': {
    top: '10px',
    left: '10px',
  },
  'navigation-control': {
    top: '46px',
    left: '10px',
  },
});

export default styles;
