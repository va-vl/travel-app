import { makeStyles } from '@material-ui/core';

const styles = makeStyles({
  'controls-wrapper': {
    position: 'relative',
    width: '29px',
    top: '10px',
    left: '10px',
  },
});

export default styles;
